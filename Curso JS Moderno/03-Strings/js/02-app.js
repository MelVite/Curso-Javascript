const product = 'Monitor 20 pulgadas';

//Métodos de Strings 

//Longitud de cadena:
console.log(product.length);

//Para determinar si una cadena incluye algo especificado, responde true o false
console.log(product.includes('2'));

//Determina el indice en que se encuentra un elemento x en el String
console.log(product.indexOf('pulgadas'));