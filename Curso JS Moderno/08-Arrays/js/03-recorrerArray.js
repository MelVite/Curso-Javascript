const months = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio'];
console.log(months);

/**
 * Para recorrer un arreglo se debe hace lo siguiente:
 * 1. Recorrer el arreglo
 * 2. Iterardor con for
 */

console.log(months.length); //muestra el número de elementos que contiene el arreglo

//iterador
for(let i = 0; i < months.length; i++) {
    console.log( months[i] );
}

