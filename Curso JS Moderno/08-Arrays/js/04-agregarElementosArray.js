//Agregar elementos a un array 
const colores = [ 'Blanco', 'Azul', 'Rojo', 'Amarillo', 'Rosa', 'Negro', 'Gris'];

console.table(colores);

//Remplazar un valor en el arreglo:
colores[0] = 'Nuevo color';
console.table(colores);

//Agregar elemento nuevo:
colores[7] = 'Ultimo color';
console.table(colores);
